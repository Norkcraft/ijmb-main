'use client';

// DashboardHeader is now handled by the DashboardShell in Dashboard.tsx
// This file is kept for backwards compatibility
export const DashboardHeader = ({ profile, user, signOut }: { profile: any; user: any; signOut: () => void }) => null;
